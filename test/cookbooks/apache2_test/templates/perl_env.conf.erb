<IfModule mod_perl.c>
  Alias /env/perl <%= @params[:app_dir] %>/perl

  <Location /env/perl>
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options ExecCGI
  </Location>
</IfModule>
